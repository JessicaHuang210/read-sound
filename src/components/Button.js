import styled from "styled-components";
import { headerBg, fsMiddle } from "utils/variables";
const Button = styled.button`
  padding: 0.7rem 2rem;
  background-color: ${headerBg};
  color: #fff;
  font-size: ${fsMiddle};
  border-radius: 2rem;
`;
export default Button;
