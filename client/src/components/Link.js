import styled from "styled-components";

const Link = styled.button`
  color: inherit;
  font-size: inherit;
  cursor: pointer;
  &:hover {
    text-decoration: underline;
  }
`;

export default Link;
